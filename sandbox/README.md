# sandbox/

The `read_file` tool can only read files under this directory. Drop any
text files you want the agent to be able to access here.

The harness will reject paths that try to escape this directory (`..`,
absolute paths, etc.).
